# uri2koru

`koru://` URI → linia DSL → `dsl2koru.dispatch()`.

```bash
uri2koru decode --uri 'koru://cmd/QUERY_REPAIR_HISTORY?project=.'
uri2koru run --uri 'koru://cmd/VALIDATE_LANE?ide=auto&instance=default'
```
