# rest2koru

REST API (FastAPI) dla `dsl2koru`. Port domyślny: **8216**.

```bash
rest2koru serve --port 8216
```

Endpointy: `POST /v1/dsl`, `POST /v1/commands`, `GET /v1/schema/{verb}`, `GET /v1/events`, `GET /health`.
