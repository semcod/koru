# uri2coru

Decode `coru://` URIs into dsl2coru command lines and dispatch them.

```bash
uri2coru decode --uri 'coru://block/repair/history?default_file=.'
uri2coru run --uri 'coru://cmd/LANE?ide=auto&instance=default'
uri2coru resolve "show repair history"
```
