# rest2coru

FastAPI REST adapter for dsl2coru.

Endpoints: `/v1/dsl`, `/v1/commands`, `/v1/schema/{verb}`, `/v1/proto`, `/v1/events`, `/health`.

Default port: **8218**.

```bash
rest2coru serve --port 8218
```
