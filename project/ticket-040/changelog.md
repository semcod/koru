# Changelog

- Planned immutable external Git adoption and frozen nested Docker installs.
- Replaced mutable nested Python installation with digest-pinned uv sync.
- Validated affected images from clean build caches and targeted runtime smoke.
