# Codex implementation record

Consolidate the duplicate DSL command-line implementations under `dsl2koru`.
Retain both console names and dialect-specific context/event-store semantics,
with `dsl2coru.cli` reduced to direct compatibility aliases.
