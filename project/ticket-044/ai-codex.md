# Codex plan

1. Pin the reusable E2E image and replace mutable pip installation with frozen uv synchronization.
2. Remove Compose package injection and require digest-pinned IDE matrix bases.
3. Run focused Docker, Compose and governance validation, then publish through protected review.
