# Changelog

- Pin the reusable example image to immutable Python and uv manifests and install its selected extras from the frozen repository lock.
- Remove mutable NLP2URI/TestQL package injection from Compose.
- Pin all IDE matrix base images and reject mutable caller-provided references.
