# Ticket 026: Adopt new-project governance 0.19.18

- **ID**: ticket-026
- **Owner**: unresolved:human
- **GitHub issue**: [#41](https://github.com/semcod/koru/issues/41)
- **Status**: PLAN
- **Workflow state**: WAIT_FOR_APPROVAL
- **Created**: 2026-09-01

## Goal and scope

Adopt the published `wellmanifest/new-project` 0.19.18 standard at immutable
revision `7dd68589340bfd4b18b94f3141f41833280c2985`. The adoption replaces the
failed and conflicting PR #43, releases false coordination leases from
integrated snapshots, assigns `packages/**` to the application workstream and
installs the current exact-revision governance lifecycle.

This is the canonical successor of the equivalent governance plan initially
allocated as ticket 025. Concurrent PR #55 reserved and delivered ticket 025
first for scan-history dedupe, so the losing plan was preserved and reallocated
without changing its outcome, implementation paths or architecture.

The standard projection is atomic: its managed files must be generated by
`goal governance adopt --upgrade`, never copied or edited individually. One
target-owned prerequisite is included: `.gitignore` will expose exactly four
managed host/workflow targets while leaving other local agent and GitHub paths
ignored. This ticket does not modify Koru runtime code and does not implement
issue #37; it only removes that issue's governance blocker.

## Planning evidence

- Current base: `main@665bc68eb22f6e98156d540a01fda6976bf0c632`.
- Current adoption: `new-project` 0.11.0 at
  `cc9b04673bbd85cb4e35fb683d288ef34be1485f`.
- Target release: 0.19.18 at
  `7dd68589340bfd4b18b94f3141f41833280c2985`; its default manifest includes
  `packages/**` application ownership introduced in 0.19.3.
- Goal 2.1.300 satisfies the upstream 2.1.295+ adoption requirement.
- Read-only preflight reports 75 atomic managed changes after the four exact
  ignored targets are made trackable.
- PR #43 is closed, conflicting and failed smoke/OneDev; it used the already
  closed ticket 021 and cannot be trusted or merged.
- GitHub now reports only `main`; all former remote topic branches were either
  merged and deleted or closed and deleted.

## Acceptance criteria

- [ ] AC-01: A human owner explicitly approves ticket 026 as the equivalent
  successor, including the exact revision and atomic generated scope.
- [ ] AC-02: `.gitignore` makes only `.aider.conf.yml`, the managed Cursor rule,
  Copilot instruction and governance workflow trackable within their otherwise
  ignored directories.
- [ ] AC-03: Goal generates the complete managed projection from published
  revision `7dd68589340bfd4b18b94f3141f41833280c2985`; a repeated `--check`
  reports no drift and the lock binds the exact source.
- [ ] AC-04: The adopted manifest owns `packages/**` as application work and
  no integrated closed ticket is treated as a live coordination lease.
- [ ] AC-05: Agent-host, ticket allocation, worktree and required-check
  contracts pass without local weakening or hand-edited managed files.
- [ ] AC-06: Governance, focused Python smoke, diff hygiene and Docker Compose
  checks pass before exact-head protected publication.

## Non-goals

- Change Koru runtime behavior or package LLM adapters.
- Reuse or force-merge the stale PR #43 head.
- Modify the published standard payload or its generated files by hand.
- Close issue #37 before its separate application implementation is delivered.

## Participants

- Human participant: unresolved; no user-* file was created by this script.
- Agent participant: [ai-codex.md](ai-codex.md)
