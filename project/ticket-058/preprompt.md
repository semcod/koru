# Preprompt

Execute the bounded URI adapter slice authorized by the user's request to
continue the plan in `docs/*`. Keep legacy imports and the console name for one
release, with canonical `uri2koru` behavior only.
