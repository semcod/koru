# Changelog

- Consolidate native and compatibility DSL dispatch in `dsl2koru`.
- Move Coru argv, runner, command, query and UI handler implementations to the
  canonical package.
- Preserve legacy imports through direct aliases with parity coverage.
