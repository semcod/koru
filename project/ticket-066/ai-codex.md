# Codex implementation record

Consolidate the duplicate DSL dispatch and handler family under `dsl2koru`.
Retain `dsl2coru` public imports as one-release direct compatibility aliases,
without changing the grammar, wire format or external Coru/IMGL behavior.
