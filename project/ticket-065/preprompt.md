Add a shell script that publishes a target pull request locally: standard-pack
conformance on the frozen head, OneDev profile test gates, REST commit statuses,
and validator-agent direct-pr dispatch without GitHub Actions on the target repo.
