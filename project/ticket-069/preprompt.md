# Preprompt

Repair the non-Git umbrella false success in `koru goal`. Resolve exactly one
contained Git repository before Goal or an agent runs, support explicit
relative `--repo`, and fail closed when automatic selection is ambiguous.
