# Changelog

- Initialized the fail-closed Goal umbrella target resolution defect ticket.
- Added safe direct/umbrella target resolution and explicit `--repo` selection.
- Added structured ambiguity refusal before Goal or remediation agent launch.
