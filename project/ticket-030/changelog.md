# Ticket Changelog (ticket-030)

## [0.1.0] - 2026-09-01

- Initial governance scaffold created.

## [0.2.0] - 2026-09-01

- Switch task profile verify to `koru ci run` and baseline to `koru ci run --skip-gates`.
- Default gate resolution to `regix` + `redup` without topology.
- Add doctor probes for `pyqual.yaml` and `scripts/ci-test.sh`.
