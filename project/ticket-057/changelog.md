# Changelog

- Replaced duplicated CLI, MCP and REST legacy implementations with warning-and-alias facades.
- Removed 246 production Python lines from the three selected legacy namespaces.
- Added conformance coverage for warnings, object identity and both CLI entry names.
