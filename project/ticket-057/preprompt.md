# Preprompt

Execute the bounded order-30 adapter slice authorized by the user's request to
continue the plan in `docs/*`. Preserve legacy import and command names for one
release, but retain no legacy business logic.
