# ticket-062 changelog

- Add `koru.queue.planfile_sync` bridge to Planfile sync integrations.
- Sync on `create_nl_task`, living status updates, scan `--apply`, and `koru work start`.
- Document `planfile_sync` policy block in init stub.