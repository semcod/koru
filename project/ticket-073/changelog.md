# Changelog

- Planned a specification-driven canonical DSL CLI with native and compatibility regression coverage.
- Replaced repeated context, format and output option declarations with immutable shared specifications.
- Unified command, script and standard-input execution while retaining the legacy empty-input help contract.
- Added command, dialect, JSON, replay and schema-validation CLI matrix coverage.
