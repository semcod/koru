# Codex plan

1. Harden the application-owned IDE matrix fixture with pinned uv and frozen lock synchronization.
2. Add an exhaustive repository audit for Dockerfile and Compose inputs, immutable bases and remote Git contexts.
3. Run focused Python and Docker smokes, managed gates and protected exact-head publication.
