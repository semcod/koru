# Preprompt

Execute the bounded NLP namespace slice authorized by the user's request to
continue the plan in `docs/*`. Preserve both package and console names for one
release, with all behavior owned by canonical `nlp2koru`.
