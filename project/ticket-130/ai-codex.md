# Agent record

`SESSION_EXECUTION_AUTHORIZATION`: the user asked to implement and test the
Goal to Planfile to Koru remediation flow.

Implementation is limited to the paths in `intent.json`. Koru validates the
strict proposal before creating a deduplicated automatic ticket and does not
accept execution authority from Goal's producer payload.
