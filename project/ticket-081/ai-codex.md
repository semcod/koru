# Authority

SESSION_EXECUTION_AUTHORIZATION: user requested repair and publication through goal -a, supplied Koru commit failure, then said “kontynuuj” after explanation that the existing changes must be split into matching tickets/worktrees. This ticket owns only queue source and tests.
