# Execution authorization

Execute the next bounded order-30 slice from
`docs/architecture/volume-reduction-plan.yaml`. The user's “kontynuuj” is
recorded as `SESSION_EXECUTION_AUTHORIZATION` for this ticket.
