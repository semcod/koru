# Changelog

- Canonicalized protobuf text encoding for both Koru project and Coru file
  contexts, leaving the legacy codec as direct aliases.
- Added one executable compatibility matrix for all six namespace pairs,
  including deprecation warnings, CLI identity and an AST behavior guard.
