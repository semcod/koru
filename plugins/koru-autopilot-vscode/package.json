{
  "name": "koru-autopilot-vscode",
  "displayName": "koru autopilot",
  "description": "Bridge between VS Code / Windsurf / Cursor and the koru autopilot daemon.",
  "version": "0.1.46",
  "publisher": "semcod",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/semcod/koru.git",
    "directory": "plugins/koru-autopilot-vscode"
  },
  "bugs": {
    "url": "https://github.com/semcod/koru/issues"
  },
  "homepage": "https://github.com/semcod/koru/tree/main/plugins/koru-autopilot-vscode#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "koru",
    "autopilot",
    "cascade",
    "windsurf",
    "cursor",
    "automation"
  ],
  "activationEvents": [
    "*",
    "onCommand:koruAutopilot.connect",
    "onCommand:koruAutopilot.sendChat",
    "onCommand:koruAutopilot.calibrateProbe",
    "onCommand:koruAutopilot.calibrate",
    "onCommand:koruAutopilot.calibrateCompact",
    "onCommand:koruAutopilot.captureSubmitClick",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "koruAutopilot.connect",
        "title": "koru: Connect autopilot daemon"
      },
      {
        "command": "koruAutopilot.sendChat",
        "title": "koru: Send text to active chat"
      },
      {
        "command": "koruAutopilot.calibrateProbe",
        "title": "koru: Calibrate chat probe ladder"
      },
      {
        "command": "koruAutopilot.calibrate",
        "title": "koru: Calibrate"
      },
      {
        "command": "koruAutopilot.calibrateCompact",
        "title": "koru:calibrate"
      },
      {
        "command": "koruAutopilot.captureSubmitClick",
        "title": "koru: Capture submit button position"
      }
    ],
    "configuration": {
      "title": "koru autopilot",
      "properties": {
        "koruAutopilot.socketPath": {
          "type": "string",
          "default": "",
          "description": "Unix socket override. Empty = KORU_AUTOPILOT_SOCKET, else KORU_AUTOPILOT_INSTANCE / XDG_RUNTIME_DIR (matches koru Python)."
        },
        "koruAutopilot.chatOpenCommands": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Commands to open AI chat, tried in order (Cursor Composer, Copilot Chat, …). Empty = extension defaults."
        },
        "koruAutopilot.autoConnect": {
          "type": "boolean",
          "default": true,
          "description": "Connect to the daemon automatically when the editor starts."
        },
        "koruAutopilot.probeLadder": {
          "type": "boolean",
          "default": true,
          "description": "Try chat commands in order with focus/paste verification; cache winning command IDs per IDE."
        },
        "koruAutopilot.probeFocusDelayMs": {
          "type": "number",
          "default": 220,
          "minimum": 50,
          "maximum": 2000,
          "description": "Milliseconds to wait after a focus-open command before verifying UI state."
        },
        "koruAutopilot.probePasteDelayMs": {
          "type": "number",
          "default": 120,
          "minimum": 50,
          "maximum": 2000,
          "description": "Milliseconds to wait after paste before checking the text did not land in a file editor."
        },
        "koruAutopilot.submitClickX": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Optional absolute screen X coordinate for clicking the chat Send button after paste. Use command `koru: Capture submit button position`."
        },
        "koruAutopilot.submitClickY": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Optional absolute screen Y coordinate for clicking the chat Send button after paste. Use command `koru: Capture submit button position`."
        },
        "koruAutopilot.trustUnverifiedHostSubmit": {
          "type": "boolean",
          "default": true,
          "description": "For VSCodium on Linux, treat a successful host Return/Ctrl+Return submit key as accepted when VS Code APIs cannot verify chat submission."
        },
        "koruAutopilot.windsurfKeepOpenAfterSend": {
          "type": "boolean",
          "default": false,
          "description": "After using Windsurf's native sendTextToChat command, run extra Cascade open commands. Disabled by default because those commands can toggle the right chat column closed when it is already open."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test": "npm run compile && node out/dispatch-plan.test.js && node out/probe-ladder.test.js && node out/antigravity-fastpath.test.js",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile",
    "package": "vsce package --no-dependencies --out koru-autopilot-${npm_package_version}.vsix",
    "publish": "vsce publish --no-dependencies",
    "clean": "rm -rf out node_modules *.vsix"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^5.4.0"
  }
}
